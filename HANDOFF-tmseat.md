# 🪑 tmseat 핸드오프 (자리후/seat 도메인 세션)

> **/clear 후 첫 턴**: 이 파일 + `~/claude-project/CONDUCTOR.md` + `thinkmap-seat/CLAUDE.md`.
> 최종 갱신: **2026-08-17 23:2x KST (`date` 실측)**. 증류판 — 140줄 → 이 길이.
> ⚠**시각은 «감각»이 아니라 «측정»이다.** 이 파일은 한때 `2026-08-18` 로 적혀 있었다 —
> 나는 `date` 를 안 돌리고 **다른 세션 블록의 시각을 보고 유추**했고, 그 블록도 실측값이 아니었다.
> **측정 안 한 값은 다른 세션의 입력이 되어 그대로 번진다.** 날짜를 적을 땐 `date` 를 돌려라.
> ★이 파일은 **«그날 무엇을 했는가»를 적는 곳이 아니다.** 그건 git log·`docs/SEAT-SPEC.md` 가 한다.
> 여기엔 **다음 세션이 모르면 손해 보는 것**만 남긴다. 늘어나면 다시 지워라.

## 정체·경계
- 자리후(seat) 도메인 세션 `tmseat`. worktree `~/claude-project/thinkmap-seat`, branch `feat/seat`.
- **커밋·푸시는 내가 한다**(origin + backup 양쪽). **배포·마이그는 게이트** — 지휘부 GO 없이 안 연다.
  자리후는 **영업 중 현장 화면**이라 배포 창(마감 후 등)을 따로 받는다.
- 인박스 `msg/to-tmseat.md` — 처리 = **헤더 앞에 `## ✅ `**. 보고 = **`msg/to-orch.md`**.
- ★★**메일박스는 `msg-edit.sh` 로만 고친다. 파이썬·에디터로 «전체 읽기 → 치환 → 전체 쓰기» 금지.**
  ```
  sh ~/claude-project/warroom/msg-edit.sh mark   msg/to-tmseat.md '<헤더 부분문자열>'
  sh ~/claude-project/warroom/msg-edit.sh append msg/to-orch.md   <본문파일>
  ```
  이유: 여러 세션이 같은 함을 통째로 덮어쓰면 **lost update** 가 난다 — 붙인 `✅` 가 원복되고,
  뗀 `✅` 가 되살아나고, **쓰기 자체가 사라진다**(2026-08-18 실물 2건). **소음이 아니라 기록 유실**이다.
  도구가 잠금·원자 교체·되읽기 확인 셋을 강제하고, `append` 는 **빈 본문을 거부**한다.
  ※이 규율은 2026-08-20 01:21 런처 주입분이라 **그 전에 기동한 세션은 못 받았다** — 내가 그랬다.
- ★★**남의 워크트리에 파일을 `cp` 하지 마라.** 이 파일은 `feat/seat` 에만 두고, `main` 쪽 사본은
  **머지로만** 간다. 전에 나는 `thinkmap/HANDOFF-tmseat.md` 로 매번 `cp` 했는데, 그건 **git 이 할 일을
  손으로 한 것**이고 결과가 나빴다: thinkmap 워크트리에 **내가 만든 dirty 파일**이 남아,
  2026-08-22 머지 사고 때 **그쪽이 «정체 모를 미커밋 수정»으로 보호하느라 시간을 썼다**(내 것인 줄 몰랐다).
  ⇒ **브랜치 간 동기화는 머지다.** 손으로 복사하면 «누구 것인지»가 사라진다.
  ★마킹 스크립트는 «미처리 전부»를 훑으므로, **작업 중 새로 들어온 블록을 안 읽고 표시할 수 있다**(2026-08-17 실제 발생).
  마킹 전에 **읽은 개수와 마킹 개수를 대조**해라.
- ★**회원님 결정이 필요하면 `to-orch` 에 «요지 + 사실»만 올린다.** 경로 = **도메인 → orch → conductor → 회원님**
  (2026-08-18 회원님 구조 판정). **`msg/to-user.md` 에 직접 발행하지 않는다** — 문면은 scribe, 발행은 conductor 다.
  · **낱장 금지** — 새 카드 대신 기존 묶음 카드의 «항목»으로 간다. 회원님 화면의 카드 수가 느는 것 자체가 비용이다.
  · **긴급 예외는 3조건이 «전부» 맞을 때만**: 사람·돈·데이터에 즉시 위험 ∧ 지연이 그 위험을 키움 ∧ 되돌리기 비용 < 지연 비용.
    이때도 **직행과 동시에** orch·conductor 통지(사후 아님).
  · 정본 = `msg/양식정본.md` §4·§8-2. ※구 규약(「각 도메인이 to-user 에 직접 발행」)은 **폐기**됐다.
- 정본 = `docs/SEAT-SPEC.md`. 손대기 전에 §15 체크리스트.

## §0-NOW — 지금만 참인 것 (★값이 썩는다. 쓰기 전에 재라)

| 무엇 | 값 | **이게 아직 참인지 재는 법** |
|---|---|---|
| ⛔**함대 «절약 모드»** | **켜짐**(2026-08-27 · 7d 98%) | `msg/to-tmseat.md` 최신 orch 블록. **판정이 바뀌면 orch 가 알린다** — 내 인박스가 표면이다 |
| 라이브 판 | `v8.18-1`(gh-pages `a56e49a`) | `scripts/deploy-seat.sh --verify` |
| 롤백 지점 | `a56e49a` | `git log -1 --format='%h %s' origin/gh-pages -- seat` |

★**절약 모드에서 줄이는 것 = «배경»만**(배경 LLM 루틴 · 교본/대장 등재 · 스윕 보고는 «바뀐 값»만).
★**안 줄이는 것 = 사용자향·안전**(배포 · 회원님 카드 · 사고 대응). ⇒ **소모가 «낭비»인지 «성과»인지로 가른다.**
※내 세션은 **인박스 구동(반응형)**이라 끌 상시 루프가 없다 — 2026-08-27 실측 0건.

## 지금 상태 (2026-08-17)
- `feat/seat` **미배포 21건**(★**커밋·push 완료, 라이브 미반영** — «미커밋»이 아니다).
  라이브 = **`v8.18-1`**(gh-pages `a56e49a`, 2026-08-25 실측) — ★**내 것이 아니라 core 배포로 갱신됐다.**
  ⇒ **라이브 판은 남이 올릴 수 있다. 내 문서의 «라이브 = …» 줄은 쓰기 전에 다시 재라.**
- 배포 노트(개선·**후퇴** 양쪽) = `docs/DEPLOY-NOTE-2026-08-17.md`. 북극성 한 장 = `docs/NORTH-STAR-seat.md`.
- ★**자리후의 라이브 표면은 하나뿐이다**: `https://jaehwan-lee-benja.github.io/thinkmap/seat/`
  근거(부재 판정의 세 줄, 2026-08-17): ⑴Edge 함수 등록부 **전수 19개** + gh-pages 트리 **전수 12폴더**를 봤다
  ⑵등록부는 **배포된 것**을 나열한다(repo 파일이 아니다) ⑶대조군 = membership `kiosk` 가 **둘 다에서 나온다**.
  ⚠**처음엔 repo grep 으로 «없음»이라 적었고 그건 무근거였다** — 같은 grep 이 membership 에서도 0건(거짓 음성).
  「라이브에 있다/없다」는 **주소를 병기**해라. 표면이 늘면 이 줄부터 고쳐라.
- 대기 중(내 손 밖): 축하 연출 2건(체크 버스트 90%·축하 색) = 회원님 결정 / 태블릿 4점 눈 확인 = 회원님 /
  훅 검증 한 겹 = 파킹.

## 손에 쥔 도구 (경로 — 전부 재실행 가능)
| 무엇 | 경로 | 언제 |
|---|---|---|
| 전체 시험 | `npx vitest run` (seat만 `… apps/seat`) | 항상. ★**`Test Files` 줄도 봐라** — 파일이 통째로 로드 실패해도 `Tests N passed` 는 초록으로 보인다(실제로 당했다) |
| 변이 시험 재현 | `apps/seat/scripts/verify-mutations.sh` (`--self-test`) | 「가드가 진짜 막나」를 의심할 때 |
| 배포 | `scripts/deploy-seat.sh` (`--verify` 만도 가능) | GO 받은 뒤 |
| 롤백 | `scripts/rollback-seat.sh` | 배포 실패 즉시. `seat/` 폴더만 되돌린다 |
| 판정 순수함수 | `utils/seatRules.js` · `seatLoadState.js` | 규칙·읽기상태·동기화 판정은 전부 여기 |
| 구조 감사기 | `utils/cssAudit.js` · `seatDevGuard.js` · `motionAudit.js` · `emptyStateAudit.js` | 픽셀로 못 보는 결함용 |

## 반복해서 부딪힌 함정
- **`OrderRow` 는 자리안내·주문서관리 공용**(현재 **11셀**). 열을 건드리면 **4곳 동기화**
  (`SEAT_COLUMNS` · `grid-template-areas` 가로/세로 · `.seat-cell-<key>` · `is-hide-<key>`). SPEC §15.
- **세로 대응은 CSS Grid 배치만** — DOM 재배열 금지(위 함정 직결).
- **스크롤 상자는 `.seat-scrollport` 하나뿐**, 패딩 0. 조상에 `overflow:hidden` 금지(**`clip` 을 써라**).
  dev 에서 `seatDevGuard` 가 콘솔로 잡는다.
- **아이콘은 `SeatIcon.jsx`** — 문자 글리프(✕·✓) 금지. 의사요소 자리는 `mask`(실패 시 **검은 네모**로 뜬다).
- seat 위성은 Material 3 예외(SPEC §12.1) → **design-guardian 비적용**. 단 **모션 정본은 적용된다**
  (`saruru-design/docs/MOTION-CANON.md`). ★**2026-08-25 재측정으로 이 줄이 갱신됐다**:
  · 정본 §11 이 신설돼 **자리후가 «이름으로» 통지처 목록에 올라갔다**(전엔 「화면 산출 전부」에만 걸려 더 조용했다).
  · **갱신 여부는 내가 잰다**: `python3 ~/claude-project/saruru-design/docs/toolkit/canon_check.py --motion`
    → 지문을 찍는다. **마지막에 본 지문 = `806a5067b83921b7`(절 15개)**. 다르면 새 절이 생긴 것이다.
  · 내 화면 판정은 `motionAudit` 이 한다. ⚠**감사기의 빨간불은 판정이 아니라 재료다**(정본 §12 판정 절차).
- **셸에서 «세는 것»은 종료코드가 개수의 함수다** — `grep … && git commit …` 사슬은 개수 0이면 **커밋이 조용히 안 된다.**
  `set -e` 아래 `$(… | grep …)` 대입은 **친절한 안내에 닿기 전에 즉사**시킨다(`|| true`).
- 훅(inbox) 오발화: 내 ✅ 기록 자체가 다음 턴 훅을 한 번 트리거할 수 있다(정상).

## 판단 기준 (내가 반복해서 되돌아온 것들)
- ★**고장이 정상 상태의 얼굴로 착지하지 않게 하라.** 빈 목록·0건을 그릴 때 근거는 **결과값이 아니라
  «그 결과를 얻는 데 성공했는가»**여야 한다. (단일점 ②, SPEC §8.1)
- ★**문서의 «전부»는 기계가 없으면 «내가 그때 본 것 전부»라는 뜻이다.** — 하루 만에 거짓으로 드러났다.
- ★**시험은 깨뜨려 봐야 시험이다.** 통과만으로는 「지켰다」와 「안 봤다」가 구별되지 않는다.
- ★**복구 경로는 한 번 돌려보기 전까지 경로가 아니다.** 리허설에서 버그 3건이 나왔다.
- ★**옳은 갈래를 아래에 적어 두는 것은 안전이 아니라 알리바이다.** 급할 때 사람은 맨 위 명령을 친다.
- ★**내가 고치는 파일에서는 내가 쓴 문구·주석이 대조군으로 잡힌다.** 감사기는 주석을 마스킹해라.
- 결정과 결함 수정을 **한 배에 묶지 마라.** 유저가 명시로 요청한 기능의 «성격»을 내 판단으로 바꾸지 않는다.

## 남은 외부 항목
- ⚠**Supabase 조직 quota — 2026-08-29 부터 프로젝트 제한 예고**(2026-07-31 대시보드 경고).
  **2026-08-25 08:3x 재측정(MCP `get_organization`·`list_projects`)**: 조직 plan **여전히 `free`** ·
  4개 중 **2개 INACTIVE**(thinksalon·todo-note) · 자리후가 쓰는 **thinkmap 은 ACTIVE_HEALTHY**.
  ⇒ **전제는 8일째 그대로 — 해소되지 않았다. 남은 기한 D-4.**
  ★08-29 에 thinkmap 이 실제로 멈추는지는 **내가 못 판정한다**(공급자 정책 = «누가 정해야 바뀌는» 쪽이라
  재도 안 바뀐다) — 지어내지 않는다. **회원님 결정 대기**이고, 카드는 낱장에서 **«맥 앞 5분» 묶음의
  «기한 있는 것» 첫 항목**으로 흡수됐다(2026-08-18 scribe).
- LAN 테스트는 **IP 말고 mDNS 이름**(`mac-mini.local:5177`)으로. Supabase Auth 리디렉트 허용목록이
  **숫자 IP 호스트를 매칭하지 못한다**(2026-07-31 로그로 실증). 이름 호스트(`localhost`·`*.local`·`*.github.io`)는 통과.
- worktree 는 `.env` 를 안 가져온다 — 새로 만들면 `~/claude-project/thinkmap/.env` 에서 복사.
- ⛔**`supabase` CLI 를 헤드리스로 부르지 마라 — 이 세션엔 `SUPABASE_ACCESS_TOKEN` 이 없다**(2026-08-25 실측).
  없으면 CLI 가 macOS **Keychain GUI 프롬프트로 폴백**해 **그냥 멎는다**(독립 실측 2건: 90초·120초×2).
  부르기 전에 **«env 가 있는지»만** 보고(⛔값은 보지 마라), 없으면 **env 를 가진 경로로 넘긴다.**
  ※내 배포 경로는 gh-pages 라 평소엔 CLI 가 필요 없다 — 이 줄은 **Edge 를 건드릴 때**를 위한 것이다.
  ★**「살아 있다」도 «어느 경로에서»가 없으면 절반만 참이다.**
