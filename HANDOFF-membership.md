# HANDOFF — membership 통합세션 인계

> 갱신: 2026-08-04 · 브랜치 `feat/membership-kiosk`(로컬 전용, 업스트림 없음) · 워킹트리 clean
> 다음 세션은 이 문서 → `docs/MEMBERSHIP-KIOSK-SPEC.md` → `~/claude-project/docs/POPCORN-LOOP-SPEC.md` 순으로 읽으면 된다.
> 인박스 `~/claude-project/msg/to-membership.md`(마지막 블록까지 전부 `✅` 처리됨) · 보고는 `msg/to-conductor.md`.

---

## 1. 지금 상태 — 한 줄

팝콘 루프(키오스크 발권 → 카운터 회수 → 손님 폰 QR)는 **코드·SPEC 모두 완성돼 있고**, 배포 대기 커밋 2개를 **유저 배포 승인 한마디**만 기다린다. 막힌 것은 기술이 아니라 게이트 3개(배포 승인 · RawBT 1분 현장 실증 · realtime authz 마이그)뿐이다.

---

## 2. ★배포 대기 커밋 (정확히 2개)

| 해시 | 내용 |
|---|---|
| `539ca4a` | docs: 검수 지적 반영 — 폐기된 기기 전제 주석 교정 + SPEC §5.1 예외 등재 |
| `fa6f6cd` | fix: 적대검증 확정 결함 8건 — ★손님 폰 바코드 잘림(핵심기능 불능) |

- **유일한 하드 게이트 = 유저 배포 승인.** 승인 나면 thinkmap 통합세션이 main 머지 → gh-pages 델타 push(`-e membership --add`) → 시각QA(768x1024 1순위). 수 분.
- ⚠️ **정정**: 8/4 to-conductor 보고에 "배포 대기 7커밋"이라 적혀 있으나 **틀렸다**. `0da6e2e·a3e9b65`는 gh-pages `c1daa86`(8/3 15:25), `a49d970·9aadbee·8cfbd33`은 `5fbb9d4`(8/3 22:49)로 **이미 배포·라이브**다. `git log main..HEAD` = 2건이 정본.
- 라이브 URL: https://jaehwan-lee-benja.github.io/thinkmap/membership/

### 배포 전제 env 3종 (미설정 = 기능이 조용히 죽는다)
`VITE_MEMBERSHIP_LIVE=1`(미설정 시 전부 미리보기) · `VITE_MEMBERSHIP_REALTIME=1`(폰 인쇄 브리지) · `VITE_MEMBERSHIP_STORE`(매장 룸 id — **키오스크와 카운터폰이 같은 값이어야 서로 만난다**. 룸 불일치가 현장 최다 실수).

---

## 3. 기기·환경 실측 정본 (유저 Fully Device Info, 2026-08-03)

| 항목 | 실측값 |
|---|---|
| 기종 | CS-273N (KICC 결제 단말 · Fully Kiosk 1.61-play, PLUS 없음) |
| OS | **Android 8.1.0 (SDK 27)** |
| 브라우저 | **WebView 126.0.6478.134 (현대 크로미움)** |
| 화면 | **768 x 1024 세로 거치** — 시각QA 1순위 뷰포트 |
| root | available·미활성 (루팅 반대 — §4) |
| 카운터 스캐너 | Newland **NLS-HR11 Plus**(HID 키보드, 폰 화면 바코드 판독 ✓). CONE-2000 Plus 예비 |
| 프린터(내장) | COM2 내부 시리얼(KICC 스택) · 용지폭 **80mm 확정** |

★ **"Android 5.1.1 / Chrome 40"은 폐기된 오가정이다.** 이 함정에 두 번 빠졌다(설계 전제로 한 번, 주석에 사실처럼 남아 재도입 유발로 또 한 번 — `539ca4a`에서 교정). **이 기기에 ES5/구형 CSS 제약은 불필요하다.** legacy 빌드는 무해하니 유지하되, **이 제약을 이유로 기능·CSS를 깎지 마라.**

---

## 4. 인쇄 경로 결정 이력

1. **키오스크 자체 인쇄 = 폐기**(2026-08-03 확정)
   - Play 프로텍트 «기기가 인증되지 않음» → RawBT **라이선스 구매/인앱결제가 원리적으로 차단**
   - KICC 내장 프린터(COM2) **종결** — 유저가 KICC·제조사 통화, "방법 모르겠다" 회신
   - `/dev/ttyS*` 직접 접근은 루트 필요 → **루팅 반대**(결제 단말 무결성·VAN 계약·AS vs 영수증 한 장 — 거래 성립 안 함)
2. **채택 = 프린터를 키오스크 밖에 둔다**
   - ⓐ **카운터 폰 브리지** `?role=printer` (키오스크 발권을 Realtime 수신 → RawBT 자동 인쇄). **realtime authz 마이그가 전제**. 설계 제약: 그 폰은 상시 유튜브 뮤직 재생용 — **오디오 포커스 탈취·포그라운드 강탈 금지**
   - ⓑ **외장 감열 프린터 80mm**(유저 구매 결정 대기)
3. **보조 = 화면 QR** `?role=ticket` (게이트 0·서버 변경 0). **종이가 없어도 회수가 된다** = 인쇄 실패가 매장을 막지 않는 무비용 백업.

★ **미확인 1건 — 판을 뒤집을 수 있다**: **무료 RawBT로도 인쇄 자체는 된다**(라이선스는 «인쇄물에 붙는 알림 문구 제거»용이고 속도·품질은 동일)는 근거를 독립 출처 2곳에서 찾았다. 단 출처가 공식 도메인이 아니라 **단정 금지**. 현장에서 막힌 것은 *"라이선스 구매가 안 된다"*였지 *"인쇄가 안 된다"*가 아니었다 — 이 둘은 다르다.
⇒ **현장 1분 실증 대기**(무료 상태로 테스트 인쇄 1회: ⑴종이가 나오는가 ⑵알림 문구가 붙는가).
**참이면 키오스크 직결이 1순위로 복귀하고 폰 브리지·realtime 마이그·유선 개조가 전부 불요가 된다.** 그때까지 폰 브리지 착수는 보류(이미 배선된 코드는 잔존).

---

## 5. 역할 6종 URL

기본 `https://jaehwan-lee-benja.github.io/thinkmap/membership/` + 쿼리. 공통 `?store=<룸id>` (미지정 시 `VITE_MEMBERSHIP_STORE`).
분기: `src/components/Kiosk/MembershipKiosk.jsx` (ticket만 `src/main.jsx` 엔트리에서 인증 앞으로 갈라짐).

| URL | 화면 | 기기 | 인증 |
|---|---|---|---|
| (없음) / `?role=customer` | CustomerView — 셀프검색·가입·발권 | 키오스크 CS-273N | 필요(매장 계정) |
| `?role=staff` | StaffView — 조회·고객푸시·회원리스트·팝콘 | 직원 노트북 | 필요 |
| `?role=scan` | ScanView — 바코드 스캔→조회·회수·인쇄 | 카운터 노트북 | 필요 |
| `?role=printer` | PrinterView — 발권 수신 자동 인쇄 브리지 | 카운터 폰 | 필요 |
| `?role=editor` | ReceiptEditor — 영수증 템플릿·테스트 인쇄 | 마스터 | 필요 |
| `?role=ticket#<base64url>` | TicketView — 실 CODE128 바코드 표시 | **손님 폰** | ★**무인증** |

★ **ticket만 무인증** — 근거는 `docs/MEMBERSHIP-KIOSK-SPEC.md` **§5.1 「★예외 1건」**: 보는 사람이 고객이라 매장 계정이 없어 게이트 뒤에 두면 원리적으로 못 연다. 안전한 이유 = **서버를 한 번도 부르지 않는다**(데이터는 URL 프래그먼트에 자족적, 프래그먼트는 서버로 전송 안 됨) ⇒ 이 절이 막으려는 «전 고객DB 노출·익명 조회 오라클»이 생성되지 않는다. 회수(redeem)는 여전히 직원 게이트 뒤 서버 판정. 수용한 위협 = 위조 URL의 «표시 사기»(재화 이득 경로 아님).

---

## 6. 미결·대기 목록

| # | 항목 | 소유 | 상태 |
|---|---|---|---|
| 1 | **유저 배포 승인** (539ca4a·fa6f6cd) | 유저 → thinkmap 통합세션 | 대기 — 유일 하드 게이트 |
| 2 | **RawBT 1분 실증** (무료판 테스트 인쇄) | 유저(현장) | 대기 — 결과가 인쇄 경로 전체를 결정 |
| 3 | **KICC 회신** (내장 프린터 개방 문의) | 유저 | **종결**(방법 없음 회신) — 재개 불요 |
| 4 | **realtime authz 마이그** (폰 브리지 전제) | 마이그 규율(guardian→유저→thinkmap) | 착수 보류(#2 결과 대기) |
| 5 | **영수증 마스킹 전화번호** | 유저 결정 | SPEC §5.A는 명시, 코드엔 없음. 넣으면 종이에 PII 한 줄 추가 — 조용히 안 바꾸고 올린다 |
| 6 | 외장 감열 프린터 80mm 구매 | 유저 | 결정 대기(#2가 참이면 불요) |
| 7 | 회원 리스트(#9) 보안 결정 (SPEC §5.2) | 유저 | 미해결 — 권고 B+C(master 전용+마스킹) |
| 8 | `docs/KIOSK-FIELD-RUNBOOK.md` | membership | 미생성(워크플로 산출물 대기) |

### 진행 중이던 백그라운드 작업 (8/4 착수, 결과 미수령 가능)
워크플로 `membership-full-sweep`(≈14 에이전트: 표면 6분할 리뷰 → 적대검증 → 런북 생성 → 3분류 종합) + 워커 3(전 역할×뷰포트 시각QA 매트릭스 / crm 계약 드리프트 대조 / 본 HANDOFF). **결과가 남아 있으면 «배포 전 필수»만 즉시 수정하고 나머지는 배포 후로 미룬다.**

---

## 7. 이 도메인에서 반복된 함정 5가지

1. **렌더됨 ≠ 읽힘.** 손님 폰 바코드는 캔버스가 그려졌지만 대부분 폰에서 잘려 **스캔 불가**였다(360/375/390/412 전부 초과, 430만 통과). 1차 QA가 canvas 374px을 **측정해 놓고도 가용 폭과 대조하지 않았다.** 바코드는 «그려졌다»가 아니라 **«실제 스캐너로 읽혔다»**가 통과 조건이다.
2. **값 대신 물리 키코드.** 매장 직원 PC는 **한글 IME가 기본**이라 스캐너 입력이 조합돼 깨진다(실측 `뮻-뮻1234`). 스캔 입력은 `e.key`가 아니라 **`e.code`(물리 키코드)로 수집**한다 — IME는 code를 바꾸지 않는다.
3. **폭·해상도는 추정하지 말고 실측.** 시뮬 3종(600x1024·720x1280·800x1280) 어디에도 없던 **768x1024**가 실기기 값이었고, 그게 «조회 버튼 잘림»의 원인이었다. 바코드 모듈폭도 상수식 대신 **부모의 실제 내부 폭을 재서** 산출해야 맞는다.
4. **확인 못 하는 성공을 주장하지 마라.** 인쇄 iframe 경로는 사실상 throw하지 않아 폴백이 **죽은 코드**였고 종이가 안 나와도 «인쇄됨»을 반환했다. ⇒ 문구를 **"요청함 — 종이 확인"**, 상태를 `printed`→`requested`로 낮췄다. 같은 규율: 근거가 없으면 «없다»가 아니라 **«공개돼 있지 않다»**로 적는다.
5. **폐기된 전제가 주석에 남아 재도입을 부른다.** "Android 5.1.1 / Chrome 40"이 코드 주석에 사실처럼 남아 미래에 ES5 제약을 되살릴 뻔했다. **전제가 뒤집히면 SPEC만이 아니라 주석·죽은 조건까지 같이 지운다.**

(보너스: 좁은 grep 술어가 **위음성**을 낸다 — 미니파이된 `orientation: portrait`를 공백 없는 패턴으로 찾아 0건이 나온 적 있다. 번들 대조는 공백·따옴표 변형에 둔감한 패턴으로.)

---

## 8. 작업 규율

- **하드 게이트**: 프로덕션 **배포·마이그·푸시**는 유저 승인 없이 하지 않는다. 다른 세션이 받은 «유저 직접 발주»는 **내 배포 승인이 아니다**(세션 간 승인 전이 금지). 게이트 직전까지 전부 끝내 두고 대기한다.
- **DB DDL**: SQL 제시 → supabase-guardian 검수 → 유저 명시 승인 → 소유 통합세션 적용. 회원 데이터는 **crm 소유**(multi-store) — 우리는 Edge 계약 소비자다. 티켓 모델은 thinkmap DB `crm` 스키마(0018/0019 적용 완료·Edge 라이브).
- **보고**: `~/claude-project/msg/to-conductor.md`에 `## [FROM membership → TO conductor · 주제 · 날짜]` 헤더로 append. 인박스 `msg/to-membership.md`는 **처리 후 헤더에 `✅`** 마킹. 전달은 복사하지 않고 "인박스 확인해"만.
- **유저 결정이 필요하면** AskUserQuestion 금지 — `to-conductor`에 옵션+권고를 쓰고 유휴 대기(지휘자가 라우팅). 사람 액션은 `msg/to-user.md`.
- **서브에이전트·워크플로 전면 허가됨**(2026-08-04). 조사·검증·스캔·렌더는 워커로 펼치고 통합세션은 **판정·커밋·보고**만 한다. **단 게이트는 불변 — 속도를 위해 게이트를 넘지 않는다.**
- 배포 후에는 **시각QA 게이트**(스크린샷 → 팔레트·잘림·정렬·콘솔) 통과해야 완료. 배포 직후 구 번들이 서빙되는 **전파 지연**이 있으니 번들 해시를 폴링해 대조한다.

---

## 부록 — 파일 지도

- 프론트: `apps/membership/src/` — `main.jsx`(ticket 분기) · `components/Kiosk/`(MembershipKiosk·CustomerView·StaffView·ScanView·PrinterView·TicketView·ReceiptEditor·MemberCard·ticketLink.js·kioskUtils.js·useMembershipChannel.js) · `api/membership.js`(LIVE 게이트) · `receipt/`(ESC/POS)
- SPEC: `docs/MEMBERSHIP-KIOSK-SPEC.md`(§5.1 인증 예외 · §5.2 리스트 보안 · §8 하드게이트) · `~/claude-project/docs/POPCORN-LOOP-SPEC.md`(§5.A 키오스크/QR · §5.B 회수/IME · §6-1 인쇄경로 결정 · §7 Phase 표)
- 2대 분리: `docs/MEMBERSHIP-2DEVICE-SPLIT-PROPOSAL.md`
- env 샘플: `.env.example` (11~14행)
