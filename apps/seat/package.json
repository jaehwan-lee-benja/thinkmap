{
  "name": "@thinkmap/seat-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "자리후(Seat) 위성 — @thinkmap/core 를 얹고 공유 Supabase(Realtime)를 읽는 독립 배포 앱. 주방 실시간 키오스크.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -e seat --add"
  },
  "dependencies": {
    "@thinkmap/core": "*",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.21"
  }
}
