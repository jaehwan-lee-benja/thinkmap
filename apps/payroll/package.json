{
  "name": "@thinkmap/payroll-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "급여(Payroll) 위성 — 모선(ThinkMap Hub)에서 떼어낸 첫 독립 배포 앱. @thinkmap/core 를 얹고 공유 Supabase 를 읽는다.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node ../../scripts/check-env-baked.mjs dist",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -e payroll --add"
  },
  "dependencies": {
    "@thinkmap/core": "*",
    "lucide-react": "^0.562.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.21"
  }
}
