# 자리후 배포 노트 — 2026-08-17 (마감 후 예정 · GO 대기)

> ★**개선과 «후퇴»를 같은 칸에 적는다**(2026-08-17 함대 형식, game 사례).
> **개선만 적으면 그건 보고가 아니라 홍보다.** 후퇴 칸이 비어 있으면 «없다»가 아니라 «안 찾았다»일 수 있으니
> 「비용은 있는데 감수한 것」까지 적는다.
>
> 대상: `feat/seat` — 미배포 11커밋. 되돌리기 = `scripts/rollback-seat.sh`(리허설 완료).

---

## 회원님 눈에 보이는 변화 — **2건**

| # | 무엇 | 어디서 체감되나 |
|---|---|---|
| 1 | **완료 반영이 즉시** — 전에는 완료를 눌러도 축하 애니메이션 0.7초가 끝나야 DB 로 나갔다 | 카이막·커피 태블릿. 다른 화면에 퍼지는 것도 그만큼 빨라진다 |
| 2 | **토스트 등장이 슬라이드 인 → 짧은 페이드** | 저장 실패 알림이 뜰 때 |

그 외 신규 기능(읽기 실패 띠·실시간 끊김 띠)은 **고장 났을 때만** 화면에 나온다 — 평시 화면은 그대로다.

## 고친 것 (개선)

| 무엇 | 왜 위험했나 |
|---|---|
| **읽기 실패가 「주문 없음」과 구별 불가**였던 것 | 로드 실패가 정상 빈 화면으로 착지 → 직원이 «주문 없구나» 하고 정상적으로 일한다 |
| **실시간 구독이 한 겹**이었던 것 | 조용히 끊기면 끊긴 시점 스냅샷을 «최신인 얼굴로» 계속 보여줬다 (→ 3겹: 구독·깨어남·60초 폴링) |
| **완료 쓰기가 애니메이션 뒤**였던 것 | 연출이 업무 임계 경로에 있었다 |
| **완료 버튼이 축하 중 `disabled`** | 카드에서 가장 큰 탭 자리가 탭을 삼켰다 |
| `raiseMethodOf`↔`optOf` **우선순위 불일치** | 드롭다운은 「야외」, 이력은 「포장」으로 갈릴 수 있었다 (실측 영향 0) |
| 배포·롤백 절차가 **문서에만** 있던 것 | 문서판 주 명령이 **다른 도메인 라이브를 죽이는** 것이었다 |

## ★후퇴 · 비용 (감수한 것)

| 무엇 | 정직한 크기 | 왜 감수했나 |
|---|---|---|
| **네트워크 요청 증가** — 안전 폴링 60초 × 훅 2개 | 태블릿당 시간당 최대 **+120건**(화면 꺼져 있으면 건너뜀) | 실시간이 조용히 죽는 것을 막는 **바닥선**. 이게 없으면 «멈춘 화면»을 아무도 모른다 |
| **축하 중 재탭하면 연출이 끊긴다** | 습관적으로 두 번 누르는 사람은 색종이를 못 본다 | 그 대가로 «가장 큰 탭 자리가 스킵을 삼키는» 문제가 사라졌다. 쓰기는 이미 나간 뒤라 **기능 손실 0** |
| **고장 시 표 영역이 줄어든다** | 띠 하나당 세로 약 40~55px | 띠를 헤더 안에 넣었더니 세로형에서 역할 탭이 두 줄로 밀려 **표가 더 줄었다**(실측). 둘 중 작은 쪽을 골랐다 |
| **완료된 카드가 700ms 더 「올라감」에 남는다** | 누른 태블릿에서만. 다른 화면은 즉시 완료로 보인다 | 연출을 안 줄이면서 쓰기를 앞당기려면 이 방법뿐이다 |
| **버전 스탬프가 자정을 넘기면 형식이 바뀐다** | `v8.17-11` → `v8.18 00:xx` | 스탬프 규칙(월.일 + 그날 커밋 수)의 성질. 카드 문면은 이미 「번호가 뭐든 볼 것은 같습니다」로 고쳐 뒀다 |

## 안 나가는 것 (결정 대기 — 결함 수정과 한 배에 안 묶는다)

- 축하 «체크 버스트» 90% 오버슈트 · 축하 색 무지개 → **회원님 결정 대기**(정본 §7-1 이 색은 면제하지 않는다).
- 자리후 태블릿 4점 눈 확인 → 회원님. 실기기 WebKit 은 내가 못 잰다.

## 배포·검증 절차

1. `scripts/deploy-seat.sh` — 계획 확인 → 빌드 → `gh-pages` 의 `seat/` **폴더만** 갱신(`-e seat --add`).
2. 자동 검증: index.html 이 가리키는 **실제** 자산 200 + 대조군 리터럴 2종 + 버전 스탬프.
   ※대조군은 «이번 판에만 있는 문구»라 `seatLoadState.test.js` 가 **문구와 대조군의 어긋남을 red 로** 잡는다.
3. 시각QA: 4역할 × 2뷰포트 스크린샷 + 콘솔(대조군 8장 확보).
4. 실패 시 `scripts/rollback-seat.sh` → `scripts/deploy-seat.sh --verify` 로 되돌아간 것을 **확인까지**.

검증 상태: seat **128 통과** · 전체 **298 통과** · 빌드 통과.
